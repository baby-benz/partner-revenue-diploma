server.port: 8082

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5432/partner_revenue
  jpa:
    hibernate:
      ddl-auto: create